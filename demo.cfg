[buildout]
extends = buildout.cfg
auto-checkout +=
    collective.contact.facetednav

[instance]
eggs +=
    collective.contact.facetednav
    collective.contact.contactlist
zcml +=
    collective.contact.facetednav
    collective.contact.contactlist
